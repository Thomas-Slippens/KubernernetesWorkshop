apiVersion: v1
kind: Service
metadata:
  name: workshop-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: workshop-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
